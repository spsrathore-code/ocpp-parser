# --- Dependencies ---
node_modules/

# --- Build output ---
dist/
build/
*.tsbuildinfo

# --- Env / secrets ---
.env
.env.*
!.env.example

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# --- Large sample logs (track via Git LFS if ever needed) ---
data/samples/*.log

# --- Orphaned working folder (empty; locked CWD prevented auto-delete) ---
OCPP Client Parser MD Collection/

# Uptime reference workbook: 13.7MB, and trimming to the raw sheets saves
# nothing (they are the bulk). Kept local; tests that use it skip when absent.
DC052_ DC053 Uptime Template.xlsx
